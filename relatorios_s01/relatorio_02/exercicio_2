function contarOcorrencias(tabela, alvo)
    local contagem = 0
    for i = 1, #tabela do
        if tabela[i] == alvo then
            contagem = contagem + 1
        end
    end
    return contagem
end

--[[
precisei de ajuda com esse return, meu código não estava funcionando
só tinha colocado end, pedi para a IA me ajuda a arrumar essa parte do
código
]]

print("Digite a quantidade de elementos (N): ")
local N = tonumber(io.read())

local numeros = {}
for i = 1, N do
    print("Digite o elemento " .. i .. ": ")
    numeros[i] = tonumber(io.read())
end

print("Digite o número X a ser buscado: ")
local X = tonumber(io.read())

local vezes = contarOcorrencias(numeros, X)
print("O número " .. X .. " aparece " .. vezes .. " vez(es) na tabela.")
